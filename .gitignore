*.html
!skannonser/web/static/**
*.csv
*.pyc
credentials.json
token.json
drive_token.json
.venv
.idea
artifacts/

# SQLite temporary files
*.db-shm
*.db-wal

# Live DB (Phase 6 Task 3): server is sole writer, backups via scheduler
main/database/properties.db
main/database/*.db-wal
main/database/*.db-shm
.env
backups/
*.egg-info/

# Nightly thumbnail cache (Phase 5 Task 5, skannonser/enrich/thumbs.py) --
# locally downloaded/regenerable, never committed.
data/thumbs/