# Sanctioned intentional differences between legacy and rebuilt parsers.
# Every entry needs a reason. `skannonser verify parse` treats matching diffs
# as explained; anything else fails the run.
#
# No entries currently: `Finnkode`/`URL` were pre-seeded as candidates (both
# parsers get their finnkode/url as caller-supplied arguments, not derived
# from the HTML, so those two fields can never diff) but the full 7,731-ad
# corpus run showed 0 allowlisted hits -- an allowlist entry that explains
# nothing is noise, so both were removed. Add a `[[allow]]` entry here only
# with the controller's sign-off, e.g.:
#
# [[allow]]
# field = "SomeField"
# reason = "why this diff is sanctioned, not a bug"
